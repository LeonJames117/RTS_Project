using UnityEngine;
using Utility_Scripts;

namespace Units.Air
{
    public class AirUnit : MobileUnit
    {
        // Start is called before the first frame update
        void Start()
        {
            unitType = SharedTypes.UnitType.Air;
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
